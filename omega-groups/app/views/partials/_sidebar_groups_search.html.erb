<%= side_bar_box(:filter) do %>
    <ul id="letter-list">
      <% ('A'..'Z').each do |l| %>
          <li>
            <span><%= link_to l, "/groups/#{l}" %></span>
          </li>
      <% end %>
    </ul>
    <div class="clear"></div>

<% end %>