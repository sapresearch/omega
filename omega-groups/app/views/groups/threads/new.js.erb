$('body').append('<%= escape_javascript(render(:partial => 'form.html')) %>');
        $('#new_group_thread').dialog({
                autoOpen:true,
            width: 500,
            title : 'Create new thread'
        }
                );