    <p class="select_registrant">
      Select a Registrant:
      <select class="contacts_list contact" >
        <% options = Contact.all.sort{|c1,c2|c1.name<=>c2.name}.map{|c|[c.name, c.id]} %>
        <% default_option_value = current_contact.id %>
        <%= options_for_select(options, default_option_value) %>
      </select>
    </p>
    