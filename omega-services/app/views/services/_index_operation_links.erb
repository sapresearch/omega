      <div class="services_left_links">
        <%= link_to "New", new_service_url %>
        <%#= link_to "Create New"+(@super_service.nil? ? "" : " Service in "+@super_service.name), new_service_url %>
      </div>
      <div class="services_right_links">
        <% unless @super_service.nil? %>
          <%= link_to "Back to "+(@super_service.is_top_level? ? "All Services" : @super_service.super_service.name),
            services_url(:super_service_id=>(@super_service.is_top_level? ? "root" : @super_service.super_service.id)), :remote=>true %>
        <% end %>
      </div>