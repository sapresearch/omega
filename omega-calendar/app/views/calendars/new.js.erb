$('body').append('<%= escape_javascript(render 'form') %>');
        $('#new_calendar').dialog({
            autoOpen    : true,
            width       : 500,
            title       : 'Create a new Calendar',
            close       :function(e,ui){
                $(this).dialog('destroy');
                $('#new_calendar').remove();
            }
        });

