$('body').append('<%= escape_javascript(render 'form', :event => @event, :calendar => @calendar) %>');


        $('.new_event').dialog({
            autoOpen    : true,
            width       : 600,
            title       : 'Create a new event',
            close       :function(e,ui){
                $(this).dialog('destroy');
                $('#new_event').remove();
            }
        });
