BEGIN:VCALENDAR
VERSION:2.0
<% @events.each do |event| %>
BEGIN:VEVENT
UID:<%= event.id %>
DTSTAMP:<%= event.created_at.to_s(:icalendar) %>
DTSTART:<%= event.start.to_s(:icalendar) %>
DTEND:<%= event.end.to_s(:icalendar) %>
SUMMARY:<%= event.title %>
DESCRIPTION:<%= event.event_description %>
URL:<%= event.url %>
END:VEVENT
<% end %>
END:VCALENDAR
