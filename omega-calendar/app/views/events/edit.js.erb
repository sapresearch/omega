$('body').append('<%= escape_javascript(render 'form.html', :event => @event, :calendar => @calendar) %>');
        $('form.edit_calendar_event').dialog({
            autoOpen    : true,
            width       : 600,
            title       : '<%= @event.name %>',
            close : function(e, ui) {

                $(this).dialog("destroy").remove();

            }

        });
        if ($('#event_all_day').is(':checked')) {
            $('.tpickr').hide();
        } else {
            $('.tpickr').show();
        }
        $('#event_all_day').change(function() {
            if ($(this).is(':checked')) {
                $('.tpickr').hide()
            } else {
                $('.tpickr').show();
            }
        });
        $('.tpickr').timepicker();