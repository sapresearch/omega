<% @contact.joined_groups.each do |g| %>
    <li>
      <%#= link_to remove_contact_contact_group_path(g, :contact_id => @contact.id), :method => :put, :remote => true do %>
      <%= link_to remove_groups_members_url(:group_id=>g.id, :member_id => @contact.id, :type=>"groups_to_contact"), :method => :delete, :remote => true do %>
          <span data-tooltip="remove <%= "#{@contact.first_name} #{@contact.last_name}"  %>  from: <%= g.name.truncate(40) %>">
      <span class="om-icon-only om-blue-icon ui-icon-minus" ></span>
     <%= g.name %>
      </span>
           <% end %>
    </li>
<% end %>
