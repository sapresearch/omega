$('#contacts_contact_pane').empty().append('<%= escape_javascript(render('show')) %>');

var contacts = <%= @contact_group.contacts.to_json.html_safe %>;

update_contacts(contacts, <%= @contact_group.id %>, "<%= @contact_group.name %>");