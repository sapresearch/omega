alert('assigned <%= @contact.id %> to <%= @contact_group.id %> with pos=<%= @group_position.id %>');
 $('#assigned_groups').empty().append('<%= escape_javascript(render :partial => 'contacts/partials/assigned_groups') %>');