<p class="explanation">
  No group is found<%= " in "+@super_group.name if @super_group %> at the moment based on the filter settings.
  <% if is_admin? %>
    <br/><br/>
    <%= om_button(:contact => true, :icon => 'clock') do %>
      <%= link_to_function 'Create a New Group'+(@super_group ? ' in '+@super_group.name : ''), "new_group('#{escape_javascript(render "groups/dialog_new_form", :group=>@new_group)}')" %>
    <% end %>
  <% end %>
</p>
