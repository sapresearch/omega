<%= side_bar_box('Volunteering Positions') do %>
    <% @positions.each do |p| %>
        <h3>
          <span class="om-icon-only om-blue-icon ui-icon-heart"></span>
          <%= link_to p.name, volunteering_position_path(p) %>
          
        </h3>
    <% end %>

<% end %>