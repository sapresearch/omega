<%= side_bar_box('Upcoming Events') do %>
    <% @events.each do |e| %>
        <h2>
          <span class="om-icon-only om-blue-icon ui-icon-calendar"></span>
          <%= e.title %>
        </h2>
    <% end %>
<% end %>